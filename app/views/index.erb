<!DOCTYPE html>
<html>
  <head>
    <title>Add Article</title>
    <link rel='stylesheet' href='/home.css'>
  </head>

  <body>
    <h1>Articles:</h1>
    <ul>
      <% @tables.each do |row| %>
        <li>
          <%= row["title"] %>
          <a href="<%=row["url"] %>"> <%= row["url"] %></a>
          <%= row["description"] %>
        </li>
        <% end %>
    </ul>
    <h3><a href="/articles/new"> Add an article!</a>
  </body>
</html>
